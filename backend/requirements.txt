# ⚠️ Versions de Python supportées : 3.11, 3.12 ou 3.13 (PAS 3.14).
# Les versions épinglées ci-dessous n'ont pas encore de wheels pré-compilés
# pour Python 3.14 (cp314) : notamment bcrypt et pydantic-core, qui devraient
# alors être compilés depuis les sources (chaîne Rust/C requise) et l'install
# échoue. Utilisez `python3.12 -m venv venv` (ou 3.11 / 3.13).

fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
bleach==6.2.0
email-validator==2.2.0

# Tests et qualité de code
pytest==8.3.4
httpx==0.28.1
pytest-cov==6.0.0
flake8==7.1.1
